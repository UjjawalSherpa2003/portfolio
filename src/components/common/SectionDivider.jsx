import React from 'react';
import styles from './SectionDivider.module.css';

const SectionDivider = () => {
  // Reset back to a simple layout spacer
  return <div className={styles.dividerWrapper} />;
};

export default SectionDivider;